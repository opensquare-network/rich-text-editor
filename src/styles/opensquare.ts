import { css } from "styled-components";

const OpenSquare = {
  wrapper: css``,
  toolbar: css``,
  tab: css``,
  tabActive: css``,
  textarea: css``,
  preview: css``
};

export default OpenSquare;
